import '../App.css';

/**
 * Componente visual de Carregamento.
 * Usa a classe CSS '.spinner' definida no App.css.
 */
function Spinner() {
    return (
        <div className="spinner"></div>
    );
}

export default Spinner;